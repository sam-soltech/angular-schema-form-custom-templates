fieldset.schema-form-fieldset(ng-disabled='form.readonly', class='{{form.htmlClass}}')
  legend(ng-class="{'sr-only': !showTitle() }") {{ form.title }}
  .help-block(ng-show='form.description', ng-bind-html='form.description')
  sf-decorator(ng-repeat='item in form.items', form='item')
